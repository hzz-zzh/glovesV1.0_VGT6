# 数据采集手套电源管理使用说明

本文档面向设备使用者，说明数据采集手套的开关机、充电、低电保护以及485上位机状态查看方法。

## 1. 日常使用

### 1.1 开机

设备处于手动关机状态时，按下电源按键即可开启传感器外设。系统会依次开启外设电源、复位IMU并恢复IMU和触觉数据采集。

如果设备处于严重低电锁定状态，直接按键不会开启外设。需要先连接Type-C电源并等待电池电压恢复，锁定解除后再按电源键。

### 1.2 关机

设备正常运行时，长按电源键约0.6秒关闭传感器外设。

关机过程会先停止IMU和触觉数据采集、清空待处理采集数据，再关闭外设电源，以避免产生不完整数据。

这里的“关机”是外设关机：IMU、触觉传感器等外设停止工作，MCU、RS485通信和电源管理仍保持运行，因此上位机仍可读取电池和系统状态。

### 1.3 电池供电

没有连接Type-C时，设备由电池供电。上位机出现以下状态属于正常现象：

```text
VBUS voltage  = 0.000 V
Input current = 0.000 A
Charge state  = NO_INPUT
```

电池电流为负值时表示电池正在放电。电流接近0A可能表示负载较小，也可能是该次电流数据无效，应结合 `current_valid` 标志判断。

### 1.4 连接Type-C充电

连接设备支持的Type-C电源后，可以保持设备运行并同时充电。正常情况下，上位机应显示：

- `vbus_present`：检测到有效外部电源；
- `charging`：当前处于充电阶段；
- 电池电流为正值：电流正在流入电池；
- 充电状态为 `CC`、`CV`、`TOPOFF` 或 `FULL`。

设备当前充电参数如下：

| 项目 | 设置值 |
|---|---:|
| 充电截止电压 | 4.40V |
| 快速充电电流 | 1.44A |
| 输入电流软件上限 | 2.50A |
| 充电终止电流 | 100mA |

BQ25622的充电电流步长为80mA，不能精确设置1.50A，因此使用不超过1.50A的最近档位1.44A。实际输入电流还会受到Type-C电源能力和板上外部ILIM电路限制。

## 2. 电量状态与自动保护

### 2.1 正常电量

正常电量时系统状态显示 `ON_NORMAL`，外设可以正常工作。

### 2.2 低电量

电池电压不高于3.50V并持续约5秒后进入低电状态。

低电状态下：

- 系统状态显示 `ON_LOW`；
- 状态标志中出现 `low`；
- 设备继续工作，不会立即关闭传感器外设。

电池电压恢复到不低于3.65V并连续保持约10秒后恢复正常状态。

当前MAX17043尚未加载该电芯的定制模型，SOC仅作为未校准估算值显示，不参与低电状态的进入、退出或严重低电保护。

### 2.3 严重低电

满足以下任一条件后进入严重低电：

- 电池电压不高于3.20V并持续约3秒；
- 任意一次有效电压测量不高于3.05V。

如果此时没有有效Type-C输入，系统会：

1. 停止IMU和触觉数据采集；
2. 清空采集队列并复位采集同步；
3. 关闭传感器外设电源；
4. 进入 `LOW_BAT_LOCKOUT` 状态，禁止直接按键重启外设。

MCU、RS485和电源管理仍继续运行，以便读取状态和检测充电恢复。

### 2.4 严重低电后的恢复

恢复步骤如下：

1. 连接Type-C电源；
2. 确认上位机出现 `vbus_present`；
3. 等待电池电压达到3.50V以上并连续保持约10秒；
4. 系统从 `LOW_BAT_LOCKOUT` 转为 `USER_OFF`；
5. 按下电源键，重新开启传感器外设。

设备不会在锁定解除后自动启动传感器，避免电池刚恢复时外设负载突然接入。

## 3. 485上位机状态说明

### 3.1 系统状态

| 数值 | 名称 | 用户含义 |
|---:|---|---|
| 0 | `INIT` | 电源管理正在初始化 |
| 1 | `ON_NORMAL` | 外设开启，电量正常 |
| 2 | `ON_LOW` | 外设开启，电量偏低 |
| 3 | `USER_OFF` | 用户已关闭外设，可按键重新开启 |
| 4 | `LOW_BAT_LOCKOUT` | 严重低电锁定，需要先插电恢复 |

### 3.2 充电状态

| 数值 | 名称 | 用户含义 |
|---:|---|---|
| 0 | `UNKNOWN` | 充电芯片状态暂时不可用 |
| 1 | `NO_INPUT` | 没有检测到有效Type-C输入 |
| 2 | `IDLE` | 已检测到输入，但当前未充电 |
| 3 | `CC` | 恒流充电 |
| 4 | `CV` | 恒压充电 |
| 5 | `TOPOFF` | 充电末段 |
| 6 | `FULL` | 电池已充满 |
| 7 | `SUSPENDED` | 因温度等条件暂停充电 |
| 8 | `FAULT` | 充电芯片报告故障 |

### 3.3 常用状态标志

| 标志 | 含义 | 使用建议 |
|---|---|---|
| `voltage_valid` | 电池电压有效 | 有效时才使用电压值 |
| `soc_valid` | SOC寄存器读取有效 | 只表示通信读取成功，不代表估算准确 |
| `current_valid` | 电池和输入电流有效 | 无此标志时不要根据0A判断充放电 |
| `vbus_present` | 有效外部电源存在 | 插入Type-C后应出现 |
| `charging` | 当前正在充电 | 通常伴随正的电池电流 |
| `low` | 电量偏低 | 建议尽快连接电源 |
| `critical` | 严重低电 | 外设可能即将关闭或已经关闭 |
| `lockout` | 严重低电锁定 | 插电恢复后再按键启动 |
| `peripheral_on` | 传感器外设已开启 | 不代表MCU是否运行 |
| `bq_comm_fault` | 充电管理芯片通信或配置异常 | 检查BQ诊断信息 |
| `gauge_comm_fault` | 电量计通信异常 | SOC可能不可信 |
| `voltage_mismatch` | 两个电压测量源相差超过100mV | 建议检查采样和连接 |
| `temp_limited` | 温度条件限制充电 | 检查电池温度检测网络 |
| `charge_fault` | 充电芯片报告故障 | 停止充电并检查故障码 |
| `safety_timer` | 充电安全计时器超时 | 检查电池、负载和充电条件 |

### 3.4 电流方向

- 电池电流为正：电池正在充电；
- 电池电流为负：电池正在放电；
- 输入电流：Type-C端进入设备的电流；
- 数据无效时，上位机可能显示0A，必须同时检查 `current_valid`。

## 4. 常见状态判断

### 4.1 未插Type-C，设备正常运行

```text
System state  : ON_NORMAL 或 ON_LOW
Charge state  : NO_INPUT
VBUS voltage  : 0.000 V
Input current : 0.000 A
```

这是正常的电池供电状态。

### 4.2 插入Type-C后仍显示NO_INPUT

依次检查：

1. Type-C电源和线缆是否正常；
2. `bq_comm_fault` 是否出现；
3. `BQ diagnostic` 是否为 `stage=0, status=0`；
4. VBUS电压是否大于0V；
5. 充电芯片或Type-C接口硬件连接是否异常。

### 4.3 显示UNKNOWN和bq_comm_fault

表示BQ25622初始化、配置或运行期读取失败。此时程序会禁止充电，但电池仍可以给系统供电。

查看 `BQ diagnostic`：

- `stage=0, status=0`：BQ工作正常；
- `stage=1 (init)`：优先检查I2C地址、供电和总线；
- `stage=3~9`：对应配置步骤写入或回读失败；
- `stage=10 (status_read)`：初始化成功，但运行期状态读取失败。

### 4.4 SOC与电压看起来不一致

MAX17043尚未加载该电芯的定制模型，因此上位机SOC标记为“未校准估算值”。当前所有低电状态与严重低电保护均依据有效电压执行，SOC偏低不会触发 `ON_LOW` 或严重低电锁定。

## 5. 使用注意事项

### MCU长期供电与自动复位

电池会持续给MCU和系统管理电路供电，外设电源按键只控制传感器外设。程序启动约3秒后开启独立硬件看门狗，随后每100毫秒刷新一次，超时时间约8秒。

- 用户关闭外设、低电锁定、IMU无数据、没有485请求以及电池充电都不会停止看门狗任务；
- MCU发生HardFault、调度器停止或CPU长期被占用时，IWDG会自动复位MCU；
- 485寄存器 `0x0046` 可查看上一次复位原因，bit3置位表示IWDG复位；
- 485寄存器 `0x0047` 可查看看门狗运行状态，正常稳定值通常为 `0x0003`。

- 使用与设备接口和设计要求匹配的Type-C电源及线缆；
- 不要仅根据SOC判断是否可以继续使用，电压过低时应及时充电；
- 出现 `charge_fault`、`temp_limited` 或 `safety_timer` 时，应停止继续测试并检查电池温度、连接和充电条件；
- 电池鼓包、异常发热、破损或有异味时，应立即停止使用和充电；
- 量产前应确认BQ25622的TS/NTC网络能够真实反映电池温度，不能用固定常温状态替代完整的电池温度保护。

## 6. 快速检查清单

设备无法开启传感器外设时，按以下顺序检查：

1. 查看系统是否为 `LOW_BAT_LOCKOUT`；
2. 查看电池电压是否低于3.50V；
3. 连接Type-C并确认 `vbus_present`；
4. 等待电压恢复和锁定解除；
5. 系统变为 `USER_OFF` 后按电源键；
6. 若仍无法启动，记录完整的电源页面、Flags、Fault code和BQ diagnostic信息。
