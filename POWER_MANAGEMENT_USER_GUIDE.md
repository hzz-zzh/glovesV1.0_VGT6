# 外部常供电模式说明

当前固件按“无电池、外部实时供电”方式运行。

## 1. 电源行为

- `PERIPH_PWR_EN` 在GPIO初始化和 `SystemManagerTask` 运行期间始终保持使能；
- IMU和触觉传感器随系统启动，不受电源按键控制；
- 电源按键仅保留为普通输入，不产生中断，也不触发开关机；
- `DISABLE_CHARGE` 固定拉低，允许硬件充电控制；固件不执行BQ25622初始化、参数配置、状态读取或故障恢复；
- MAX17043不执行初始化和电压/SOC读取；
- 不执行低电、严重低电、锁定或电池恢复判断；
- 不会因为电池、充电或电源状态停止IMU和触觉采集。

## 2. 485电源状态

为了保持现有Modbus寄存器布局兼容，`0x0060～0x0071` 电源状态区继续保留，但返回固定状态：

| 项目 | 固定值 |
|---|---:|
| 电池电压、电流、SOC | 0，且对应有效位不置位 |
| VBUS电压、输入电流 | 0，且对应有效位不置位 |
| system power state | `1`，即 `ON_NORMAL` |
| charger state | `0`，即 `UNKNOWN`/未启用 |
| power flags | `0x0100`，仅 `peripheral_on` |
| fault、BQ诊断和事件 | 0 |

这些固定值只是兼容原有上位机协议，不代表固件仍在读取电池或充电芯片。

## 3. 硬件要求

外部电源必须满足主控和全部传感器同时工作的电压、电流及纹波要求。当前固件不会检测外部电源欠压，也不会在供电异常时主动关闭外设，因此欠压、过流和反接保护需要由供电硬件保证。
