# Glove Modbus485 协议

本文档描述当前固件开放给主站的Modbus RTU寄存器和自定义功能码。寄存器地址均为16位Holding Register地址。

- 文档修订：`2.3`
- Modbus协议版本：`0x0202`（主版本2，次版本2）
- Health协议版本：`0x0201`（主版本2，次版本1）
- 传感器快照Schema：`3`
- 固件版本：`V3.1.2`
- 兼容性：本版本移除`0x0060..0x0071`，并修改自定义`0x41`的元数据语义；旧版主站必须同步升级

## 文档更新日志

更新日志按正式发布记录，仅说明通信接口、数据语义及主站使用流程的变化。

| 文档修订 | 日期 | 固件版本 | Modbus/Health版本 | 快照Schema | 变更性质 |
|---|---|---|---|---:|---|
| `2.3` | `2026-09-17` | `V3.1.2` | `0x0202 / 0x0201` | `3` | 增加独立调试命令、会话、状态区及无效UTC语义；0x41布局不变 |
| `2.2` | `2026-09-17` | `V3.1.1` | `0x0201 / 0x0201` | `3` | 4Mbps通信、125Hz快照更新、一次性PPS对时及逐帧UTC有效性；接口布局不变 |
| `2.1` | `2026-09-15` | `V3.1.0` | `0x0201 / 0x0201` | `3` | PPS状态、失效标志和高速快照协议升级 |

修订`2.3`的主要变化：

- Modbus升至0x0202，能力新增bit4；Health仍为0x0201、Schema仍为3，0x41长度和布局不变。
- 增加启动、停止、续期命令及0x0026..0x002F状态区；工作模式增加独立调试和切换状态。
- 快照bit4标识独立调试，该模式允许无PPS有效数据，但UTC无效且绝对时间字段为0，禁止UTC写入和SD录制。
- 增加10秒调试会话；退出后需新的PPS和UTC确认，正式PPS模式流程不变。
- 固件版本寄存器更新为3 / 1 / 2。

修订`2.2`的主要变化：

- 默认RS485波特率由6000000改为4000000 bit/s，串口格式仍为8N1；主站需同步调整通信参数。
- 传感器快照标称更新率由200Hz改为125Hz，间隔由5ms改为8ms，每个正常PPS周期产生125个采样点；主站建议使用8ms轮询周期。
- UTC采用首次及PPS失步恢复后一次性对时：主站在PPS之后写入该边沿对应的UTC整秒（nsec=0）；连续正常PPS期间无需每秒写UTC，UTC有效状态保持。主动校时及同一PPS的UTC报文重试仍兼容。
- `0x41`的UTC有效位表示本帧采集时UTC有效，且读取时仍处于同步状态；首次/恢复对时前采集的旧帧不会因后续UTC写入而被标记为有效。传感器数据有效性与UTC有效性独立。
- 每秒最后一点之后等待下一PPS时，`ACQ_ACTIVE=0`为正常状态，不代表本帧数据或UTC无效；主站应分别判断数据有效位、UTC有效位、PPS状态及帧龄。
- SD V2记录偏移17的`time_synced`表示本帧采集时的UTC有效性；raw/full/processed有效标志增加bit5（`0x20`）表示本帧UTC有效。记录布局不变。

修订`2.1`的主要变化：

- Health增加`source=6`、ready bit4、active flag bit11和错误码`0x5003`，用于报告PPS输入状态。
- `0x41`快照增加`PPS_PRESENT`、`ACQ_ACTIVE`及`FRAME_AGE_MS`；载荷由904字节变为906字节，总帧长由910字节变为912字节。
- PPS缺失时停止更新传感器数据，但RS485继续响应；保留最后一帧数值供诊断，同时清除数据及UTC有效位，主站必须依据有效位和帧龄决定是否使用数据。
- PPS恢复后需重新写入对应UTC整秒，恢复前的估算时间不得视为有效UTC。

## 1. 通讯基础

- 物理层：RS485
- 协议：Modbus RTU
- 从站地址：默认 `0x01`
- 串口格式：`8N1`
- 当前源码波特率：`4000000 bit/s`
- 支持功能码：
  - `0x03`：Read Holding Registers
  - `0x06`：Write Single Register
  - `0x10`：Write Multiple Registers
  - `0x41`：Read Sensor Snapshot，自定义只读功能码
- 单次读寄存器数量：`1..125`
- `0x10` 单次写寄存器数量：`1..123`
- `0x06` 单次写寄存器数量：固定 `1`

## 2. 数据格式

### U16

单个 Modbus register，寄存器内按 Modbus 标准高字节在前。

### U32 / U64

分别占2个或4个寄存器，低16位寄存器在前；每个寄存器内部仍按Modbus标准高字节在前。

### ROS Time

占 4 个寄存器，低 16 位寄存器在前：

| offset | 含义 |
|---:|---|
| `+0` | sec bit15..0 |
| `+1` | sec bit31..16 |
| `+2` | nanosec bit15..0 |
| `+3` | nanosec bit31..16 |

`nanosec` 必须小于 `1000000000`。

### float32

占 2 个寄存器，低 16 位寄存器在前：

| offset | 含义 |
|---:|---|
| `+0` | float bit15..0 |
| `+1` | float bit31..16 |

示例：`1.0f = 0x3F800000`，寄存器顺序为 `0x0000, 0x3F80`。

## 3. 可读寄存器

### 3.1 基础状态区

| 地址 | 数量 | 类型 | 含义 |
|---:|---:|---|---|
| `0x0000` | 1 | U16 | 从站地址，默认 `1` |
| `0x0001` | 1 | U16 | 波特率代码，当前返回 `0` |
| `0x0002` | 4 | ROS Time | 当前 UTC 时间 |
| `0x0006` | 4 | ROS Time | 本地运行时间，按 sec/nanosec 表示 |
| `0x000A` | 4 | ROS Time | 最近一次主站同步的 UTC 时间 |

#### 3.1.1 固件版本信息区

固件版本采用 `V主版本.次版本.修订版本` 格式。三个寄存器均为只读，
当前版本为 `V3.1.2`。

| 地址 | 数量 | 类型 | 含义 |
|---:|---:|---|---|
| `0x000E` | 1 | U16 | 固件主版本，当前为 `3` |
| `0x000F` | 1 | U16 | 固件次版本，当前为 `1` |
| `0x0010` | 1 | U16 | 固件修订版本，当前为 `2` |

#### 3.1.2 协议和设备信息区

| 地址 | 数量 | 类型 | 含义 |
|---:|---:|---|---|
| `0x0011` | 1 | U16 | Modbus协议版本，当前`0x0202` |
| `0x0012` | 1 | U16 | `0x41`传感器快照Schema，当前`3` |
| `0x0013` | 1 | U16 | 设备能力位图 |
| `0x0014` | 1 | U16 | 手套侧别：`0=未知`、`1=左手`、`2=右手` |
| `0x0015` | 1 | U16 | 硬件版本；未烧录板级版本时返回`0` |
| `0x0016..0x001B` | 6 | 3×U32 | MCU 96位唯一标识，每个U32低16位寄存器在前 |

设备能力位图：bit0=`0x41`传感器快照、bit1=UTC时间同步、bit2=IMU校准、bit3=SD日志、bit4=独立调试采集。主站应先检查协议版本和能力位，再启用相应功能。

### 3.2 命令状态区

`0x0020..0x0022`可写，`0x0023..0x0025`只读。命令采用应用层ACK，Modbus写应答成功只表示寄存器写入成功，主站仍需读取ACK区判断命令结果。

| 地址 | 数量 | 访问 | 含义 |
|---:|---:|---|---|
| `0x0020` | 1 | R/W | command；执行后自动清零 |
| `0x0021` | 1 | R/W | command param；执行后自动清零 |
| `0x0022` | 1 | R/W | command seq；保留最近写入值 |
| `0x0023` | 1 | R | command ack |
| `0x0024` | 1 | R | command ack seq |
| `0x0025` | 1 | R | command error detail |
| `0x0026..0x002F` | 10 | 混合 | 独立调试状态，只读，见下文 |
| `0x0030..0x003E` | 15 | U16 | reserved，当前读 `0` |

ACK定义：`0x0000=idle`、`0x0001=ok`、`0x0002=busy`、`0x8001=unknown command`、`0x8002=invalid param`、`0x8003=state denied`、`0x8004=failed`。

error detail定义：`0x0000=none`、`0x0001=invalid param`、`0x0002=duplicate seq`、`0x0003=state denied`、`0x0004=resource not ready`、`0x0005=start failed`、`0x0006=stop failed`、`0x0007=timeout`。相同`seq`的重发不会重复执行命令，而是保留上一笔ACK和error结果。

### 3.2.1 独立调试采集接口

用于无PPS时测试手套，不替代PPS对齐、UTC或SD联调。需确认Modbus版本不低于`0x0202`且能力bit4置位。用`0x10`一次写入`0x0020..0x0022`（cmd、param、seq）：

| cmd | param | 行为 |
|---:|---:|---|
| `0x0501` | `0xD06B` | 申请启动125Hz独立调试；已在调试时刷新会话 |
| `0x0502` | `0` | 申请退出调试；可取消尚未完成的启动 |
| `0x0503` | `0xD06B` | 续期；正常模式、正在退出或会话已到期时拒绝 |

ACK=1仅表示申请接受，不表示切换完成或传感器正常；读取0x0026或0x0041确认实际模式，再检查0x41有效位及帧号。切换忙返回ACK=2；状态不允许返回ACK=0x8003、error=0x0003。重发同一笔用相同seq，新命令和每次续期必须使用新seq。

只读状态区：

| 地址 | 数量 | 类型 | 含义 |
|---:|---:|---|---|
| `0x0026` | 1 | U16 | 0=正常PPS，1=独立调试，2=切换中；与0x0041一致 |
| `0x0027` | 1 | U16 | 会话剩余ms，最大10000；非调试会话为0 |
| `0x0028` | 2 | U32 | 采集代次，模式切换或正常模式PPS丢失时递增 |
| `0x002A` | 2 | U32 | 最近采样触发序号，不等同于0x41完整帧号 |
| `0x002C` | 4 | U64 | 最近触发的本地运行时间，单位us，不是UTC |

U32/U64按低16位寄存器在前。一次读完10个寄存器获得一致快照；最近触发可能领先0x41完成帧，不可把它当作该帧时间戳。

- 会话10秒，建议每3秒续期；普通0x41读取不续期。到期自动退出，复位后进入正常模式。
- 调试复用真实采集链路，0x41布局和长度不变；数据完整且新鲜时，无PPS也可有效。
- 快照bit4为DEBUG_MODE，bit3为ACQ_ACTIVE；UTC_VALID固定为0，0x41及IMU/关节/触觉绝对时间字段为0，本地时间仅通过诊断区提供。
- PPS存在位只反映真实输入；调试期间PPS不会接管采样。退出后等待新的PPS，并重新写入对应UTC。
- 切换期间数据无效，保留旧数值供诊断；首个新模式完整帧后恢复有效，帧号不清零，切换期间不保证8ms连续性。
- 录制、准备或停止录制时拒绝启动调试；调试和切换期间拒绝SD启动及UTC写入，UTC写入返回Modbus异常03。
- 参数0xD06B仅防误操作，不是身份认证；限受控维护连接使用，不能把调试数据用于正式同步控制。

### 3.3 系统状态区

| 地址 | 数量 | 类型 | 含义 |
|---:|---:|---|---|
| `0x0040` | 1 | U16 | system_state，与Health overall state一致 |
| `0x0041` | 1 | U16 | work_mode：`0=normal`、`1=standalone debug`、`2=switching` |
| `0x0042` | 1 | U16 | log_state：`0=idle`、`1=recording` |
| `0x0043` | 1 | U16 | sd_state：`0=not ready`、`1=ready` |
| `0x0044` | 1 | U16 | sensor_state，IMU 有效位，bit0 对应 IMU0 |
| `0x0045` | 1 | U16 | comm_state：`1=ok`、`2=degraded` |
| `0x0046` | 1 | U16 | MCU复位原因位图 |
| `0x0047` | 1 | U16 | MCU独立看门狗状态位图 |
| `0x0048` | 2 | float32 | board temperature，当前占位 `25.0` |

`0x0046`：bit0=引脚复位、bit1=上电或欠压复位、bit2=软件复位、bit3=IWDG复位、bit4=WWDG复位、bit5=低功耗复位。多个原因可能同时存在。

`0x0047`：bit0=IWDG已经启动、bit1=已经执行硬件刷新、bit2=IWDG配置回读警告。正常稳定运行时通常为 `0x0003`；若为 `0x0007`，表示预分频或重装值回读不一致，但任务仍会每100毫秒刷新硬件狗。

### 3.4 统一健康状态区

范围：`0x004A..0x005F`，共22个寄存器。32位量均为低16位寄存器在前。

| 地址 | 数量 | 类型 | 含义 |
|---:|---:|---|---|
| `0x004A` | 1 | U16 | Health协议版本，当前`0x0201` |
| `0x004B` | 1 | U16 | overall state |
| `0x004C` | 2 | U32 | active flags，低字在前 |
| `0x004E` | 1 | U16 | current error |
| `0x004F` | 1 | U16 | current source |
| `0x0050` | 1 | U16 | current target |
| `0x0051` | 1 | U16 | recovery stage |
| `0x0052` | 1 | U16 | 高8位=attempt limit，低8位=attempt |
| `0x0053` | 1 | U16 | last error |
| `0x0054` | 1 | U16 | last source |
| `0x0055` | 1 | U16 | last target |
| `0x0056` | 2 | U32 | error sequence，低字在前 |
| `0x0058` | 2 | U32 | error count，低字在前 |
| `0x005A` | 2 | U32 | last error uptime，单位ms，低字在前 |
| `0x005C` | 1 | U16 | live IMU mask，bit0对应IMU0 |
| `0x005D` | 1 | U16 | sensor ready flags |
| `0x005E` | 1 | U16 | FullFrame age，单位ms；`0xFFFF`表示不可用 |
| `0x005F` | 1 | U16 | 最近一次HAL UART错误位 |

overall state：`0=INIT`、`1=OK`、`2=WARNING`、`3=DEGRADED`、`4=RECOVERING`、`5=FAULT`。

source：`0=none`、`1=IMU`、`2=CAN1`、`3=CAN2`、`4=touch`、`5=pipeline`、`6=acquisition`、`9=watchdog`、`10=RS485`、`11=time sync`、`12=calibration`、`13=storage`。编号`7..8`保留。

recovery stage：`0=none`、`1=loss confirm`、`2=node config`、`3=node verify`、`4=bus reinit`、`5=bus config`、`6=bus verify`、`12=failed`。编号`7..11`保留，不再分配。

sensor ready flags：bit0=全部IMU、bit1=触觉、bit2=FullFrame、bit3=关节、bit4=PPS当前存在、bit5=时间同步、bit6=RS485。

active flags：

| 位 | 含义 | 位 | 含义 |
|---:|---|---:|---|
| 0 | IMU部分失效 | 14 | 保留 |
| 1 | 全部IMU失效 | 15 | 保留 |
| 2 | 触觉无效 | 16 | 保留 |
| 3 | FullFrame过期 | 17 | 保留 |
| 4 | 关节无效 | 18 | 保留 |
| 5 | CAN1 error-passive | 19 | 看门狗警告 |
| 6 | CAN1 bus-off | 20 | 时间未同步/失步 |
| 7 | CAN2 error-passive | 21 | 校准错误 |
| 8 | CAN2 bus-off | 22 | RS485接收覆盖 |
| 9 | IMU配置失败 | 23 | RS485 UART错误 |
| 10 | CAN重初始化失败 | 24 | RS485发送失败 |
| 11 | PPS丢失 | 25 | 队列压力 |
| 12 | 保留 | 26 | 内存池耗尽 |
| 13 | 保留 | 27 | SD错误 |

错误码：

| 范围/值 | 含义 |
|---:|---|
| `0x1001` | IMU节点数据过期，target为逻辑节点 |
| `0x1002` | IMU节点配置失败，target为逻辑节点 |
| `0x2001` | CAN error-passive，target为总线号 |
| `0x2002` | CAN bus-off，target为总线号 |
| `0x2003` | CAN重新初始化失败 |
| `0x2004` | CAN恢复验证失败 |
| `0x3001` | 触觉同步超时 |
| `0x3002` | 触觉ADC DMA超时 |
| `0x3003` | 触觉ADC DMA错误 |
| `0x4001` | FullFrame过期 |
| `0x4002` | IMU/触觉时间戳不匹配 |
| `0x4003` | 数据队列满 |
| `0x4004` | 数据池耗尽 |
| `0x4005` | 关节算法输入无效 |
| `0x5001` | 采集暂停超时 |
| `0x5002` | 同步启动失败 |
| `0x5003` | 已经收到过PPS，但超过预计周期加50ms仍未收到下一沿 |
| `0x7001` | 看门狗配置警告 |
| `0x8001` | RS485接收帧覆盖 |
| `0x8002` | RS485 UART错误，target为HAL UART错误位 |
| `0x8003` | RS485发送失败 |
| `0x8004` | 时间同步丢失 |
| `0x9001` | 校准被拒绝，target为条目编号 |
| `0xA001` | SD错误，target为SD错误码 |

错误码`0x6001..0x6007`为历史版本保留值，当前固件不会产生。

`0x005F` UART错误位：bit0=PE、bit1=NE、bit2=FE、bit3=ORE、bit4=DMA、bit5=RTO。

`current_*`和active flags表示当前仍存在的问题，经过验证恢复后自动清除；`last_*`、sequence、count和last uptime为历史信息，直到主站执行历史清除命令或设备复位。

### 3.5 退役地址区

`0x0060..0x0071`不再定义。对该范围执行`0x03`读取会返回Modbus异常`0x02`（Illegal Data Address）。主站不得把该范围的零值解释为有效状态。

### 3.6 SD状态区

范围：`0x0081..0x00BF`。状态来自`SdLog_GetStatus()`实时快照，不是固定占位值。

| 地址 | 数量 | 类型 | 含义 |
|---:|---:|---|---|
| `0x0081` | 1 | U16 | 文件系统状态：`0=未挂载`、`1=已挂载`；停止录制完成后返回未挂载 |
| `0x0082` | 1 | U16 | 记录状态：`0=idle`、`1=recording`、`2=stopping`、`3=preparing`、`0x8000=error` |
| `0x0083` | 1 | U16 | SD错误码；`0x8001=非exFAT`、`0x8002=短写`、`0x8003=文件号用尽`、`0x8004=UTC未同步`，其他值为FatFs错误 |
| `0x0084` | 2 | U32 | 总容量MB，低字在前 |
| `0x0086` | 2 | U32 | 剩余容量MB，低字在前 |
| `0x0088` | 2 | U32 | 已用容量MB，低字在前 |
| `0x008A` | 1 | U16 | 当前文件ID |
| `0x008C` | 4 | U64 | 当前文件大小，低字在前 |
| `0x0090` | 2 | U32 | 当前写入次数，低字在前 |
| `0x0092` | 1 | U16 | 日志格式版本，当前为 `2` |
| `0x0093` | 1 | U16 | 单帧记录字节数，当前为 `1536` |
| `0x0094` | 2 | U32 | 最近一次批量写入耗时ms，低字在前 |
| `0x0096` | 2 | U32 | 本次录制最大批量写入耗时ms，低字在前 |
| `0x0098` | 2 | U32 | 慢写次数；4帧批量写入超过20ms时加1 |
| `0x009A` | 4 | U64 | 日志长度，低字在前 |
| `0x00A0` | 16 | text | 当前文件名，每个寄存器高字节字符在前 |
| `0x00B0` | 16 | text | 最近文件名，每个寄存器高字节字符在前 |

日志V2每帧固定1536字节，结构如下：

| offset | 字节数 | 含义 |
|---:|---:|---|
| `0` | 4 | magic=`GLV2` |
| `4` | 12 | 格式版本、头长度、记录长度、IMU/关节/触觉数量，各为U16小端 |
| `16` | 8 | 左右手、时间同步标志、固件主/次/修订版本 |
| `24` | 72 | frame_id、UTC时间戳、Full/Raw/Processed有效标志、IMU有效掩码、算法状态、标定序号/应用状态、IMU/触觉源序号和源时间戳、Health状态 |
| `96` | 640 | 16路IMU，每路10个float32小端 |
| `736` | 108 | 27个关节角float32小端 |
| `844` | 136 | 68点触觉值U16小端 |
| `980` | 136 | 68点触觉baseline U16小端 |
| `1116` | 417 | 保留并补0 |
| `1533` | 2 | 前1533字节的Modbus CRC16，小端 |
| `1535` | 1 | 分隔字节0 |

固件以4帧为一批写入，并在底层对齐时使用SDMMC多块DMA。文件创建完成后才开启
Storage数据投递，停止时先关闭新帧投递，再写完已经进入队列的数据。文件按实际数据量
自然扩展，不使用固定大小预分配，因此长时间记录不受预分配长度限制，异常断电也不会
遗留整块预分配空白区。Storage消费者队列可暂存128帧，对应125Hz下1024ms的写卡延迟缓冲；RS485队列仍保持
4帧，FullFrame池共136块并保留发布过程余量。125Hz下持续数据率为
`1536 × 125 = 192000 byte/s`，约`0.691 GB/hour`（十进制）或`0.644 GiB/hour`；
该带宽远低于正常SD卡顺序写能力，实际风险主要来自卡内部整理造成的单次写延迟尖峰。
量产测试应观察`0x0096`和`0x0098..0x0099`：4帧批写慢写统计阈值仍为20ms，125Hz下正常补齐4帧约需32ms；
单次写入接近1024ms时可能耗尽初始为空的128帧队列，已有积压时更早耗尽，不能仅凭平均写入速度判断卡型合格。

创建文件前必须完成UTC同步。FatFs目录时间统一写UTC，`tools/modbus485_monitor.py`的
“Start Recording”会先同步主机UTC，再下发开始录制命令。解析工具
`tools/parse_sd_log.py`按流式方式读取V2文件并输出frames/imu/joint/tactile四个CSV，
同时检查frame_id连续性、重复/倒序帧、时间戳倒退、CRC和异常断电文件尾。

### 3.7 工作状态

| 地址 | 数量 | 类型 | 含义 |
|---:|---:|---|---|
| `0x0500` | 1 | U16 | work_state：`0=idle`、`1=acquiring`、`0x8000=error` |

### 3.8 IMU 原始数据区

范围：`0x1000..0x113F`，共 16 个 IMU，每个 IMU 20 个寄存器，即 10 个 float32。

第 `i` 个 IMU 的基地址：

```text
imu_base = 0x1000 + i * 20, i = 0..15
```

| offset | 数量 | 类型 | 含义 | 单位 |
|---:|---:|---|---|---|
| `+0` | 2 | float32 | acc_x | m/s^2 |
| `+2` | 2 | float32 | acc_y | m/s^2 |
| `+4` | 2 | float32 | acc_z | m/s^2 |
| `+6` | 2 | float32 | gyro_x | rad/s |
| `+8` | 2 | float32 | gyro_y | rad/s |
| `+10` | 2 | float32 | gyro_z | rad/s |
| `+12` | 2 | float32 | quat_w | - |
| `+14` | 2 | float32 | quat_x | - |
| `+16` | 2 | float32 | quat_y | - |
| `+18` | 2 | float32 | quat_z | - |

说明：`0x1000..0x113F` 始终返回 IMU 采集后的 raw 数据，校准表不会改写这里的加速度、角速度或四元数。校准只影响后续关节角解算结果。

IMU 状态：

| 地址 | 数量 | 类型 | 含义 |
|---:|---:|---|---|
| `0x1140` | 4 | ROS Time | IMU 数据时间戳 |
| `0x1144` | 1 | U16 | IMU 有效位，bit0 对应 IMU0，bit15 对应 IMU15 |

单个IMU的加速度、陀螺仪或四元数任一项超过100ms未更新时，`0x1144`对应有效位清零。数据寄存器保留最后一帧供诊断，主站不得在有效位清零后继续用于控制。恢复收到完整数据后自动恢复有效位和实时数值。

说明：正常模式的`0x1140`、`0x1340`、`0x2080`三个数据区时间戳均表示同一帧的采集时刻UTC，三个值应保持一致。关节时间戳不是解算完成时刻，而是输入数据的采集时刻。正常模式从未收到UTC基准前时间戳为`0.000000000`；已有基准但失步时可保留估算值，UTC有效位为0时只能视为未同步时间。独立调试及模式切换期间这三个时间戳固定为0。

### 3.9 关节解算数据区

范围：`0x1300..0x1335`，共 27 个 float32。

第 `j` 个输出的地址：

```text
joint_addr = 0x1300 + j * 2, j = 0..26
```

| index | 类型 | 含义 |
|---:|---|---|
| `0..15` | float32 | 食指/中指/无名指/小指，每指 4 个输出：MCP flex, MCP swing, PIP flex, DIP flex，单位 deg |
| `16..18` | float32 | 拇指 MCP flex, MCP swing, IP flex，单位 deg |
| `19..22` | float32 | 拇指 CMC quaternion：w, x, y, z |
| `23..26` | float32 | 手掌绝对 quaternion：w, x, y, z |

关节状态：

| 地址 | 数量 | 类型 | 含义 |
|---:|---:|---|---|
| `0x1340` | 4 | ROS Time | 关节数据时间戳 |
| `0x1344` | 1 | U16 | status_flags，bit0=snapshot_valid，bit1=algorithm_valid，bit2=joint_calib_applied |
| `0x1345` | 1 | U16 | joint_valid_bits low，bit0 对应 joint0 |
| `0x1346` | 1 | U16 | joint_valid_bits high |

### 3.10 触觉矩阵数据区

范围：`0x2000..0x207F`，容量 128 个 U16；当前有效点数为 68。

第 `k` 个触觉点的地址：

```text
touch_addr = 0x2000 + k, k = 0..67
```

| 地址 | 数量 | 类型 | 含义 |
|---:|---:|---|---|
| `0x2000..0x2043` | 68 | U16 | touch[0..67] 原始采样值 |
| `0x2044..0x207F` | 60 | U16 | reserved，当前读 `0` |
| `0x2080` | 4 | ROS Time | 触觉数据时间戳 |
| `0x2084` | 1 | U16 | status_flags，bit0=snapshot_valid，bit1=touch_valid |
| `0x2085` | 1 | U16 | 当前有效触觉点数，固定 `68` |
| `0x2086` | 1 | U16 | 触觉区容量，固定 `128` |
| `0x2087` | 1 | U16 | reserved，当前读 `0` |

触觉 index 简要说明：

- `0..19`：5 个手指，每指 4 点，`index = finger * 4 + point`
- `20..67`：掌心 6 列 x 8 行，`index = 20 + palm_col * 8 + palm_row`

## 4. 可写寄存器

当前开放 `0x10 Write Multiple Registers` 和 `0x06 Write Single Register` 写入以下区域。`0x10` 用于批量写表和推荐的 apply/reset；`0x06` 用于兼容通用 Modbus 工具的单寄存器写。

### 4.1 清除历史错误

命令值：`0x004A`；保护魔数：`0xC1EA`。

推荐使用FC10原子写入：

```text
Write Multiple Registers
start = 0x0020
count = 3
data  = 0x004A, 0xC1EA, seq
```

随后读取：

```text
Read Holding Registers
start = 0x0023
count = 3
```

成功条件：`ACK=0x0001`、`ACK_SEQ=seq`、`ERROR=0x0000`。

也支持FC06，但必须按参数、序号、命令的顺序写，最后一笔写命令才会触发执行：

```text
Write Single Register 0x0021 = 0xC1EA
Write Single Register 0x0022 = seq
Write Single Register 0x0020 = 0x004A
```

清除范围：`last error/source/target`、error sequence、error count、last error uptime和最近UART错误细节。不会清除active flags、current error/source/target、恢复阶段、IMU在线掩码或Ready flags；当前故障仍存在时继续对外显示。命令不会写入Flash，设备复位同样会清空历史。

主站每次发起新的命令必须更换`seq`；相同`seq`被视为重发，不会重复执行。

### 4.2 SD录制控制

开始录制命令为`0x0094`，停止录制命令为`0x0096`，两者参数均必须为`0`。
推荐使用FC10原子写入：

```text
start = 0x0020
count = 3
data  = command, 0x0000, seq
```

写入后读取`0x0023..0x0025`，`ACK=0x0001`、`ACK_SEQ=seq`、`ERROR=0x0000`
表示命令已经进入Storage控制队列。主站随后必须轮询`0x0082..0x0083`：开始录制等待
状态进入`recording`，停止录制等待状态进入`idle`；进入`error`表示实际执行失败。
开始命令会挂载exFAT分区并创建下一个`LOGxxxx.BIN`；UTC未同步时不允许入队并返回
`resource not ready`。停止命令会先关闭新帧投递，再处理已经进入存储队列的帧，最后
刷新批缓存、执行`f_sync`、关闭文件、卸载FatFs并反初始化SDMMC。下一次开始命令会
重新初始化SDMMC、挂载当前插入的exFAT卡、扫描日志文件编号并创建新文件，不复用上一次
介质会话中的缓存状态。

用户关闭传感器外设、严重低电保护或整机外设恢复流程进入安全停机时，系统也会自动
执行相同的停止步骤。突然拔卡、硬断电或MCU硬故障无法依靠软件关闭文件，量产测试仍需
覆盖异常断电后的文件系统和末尾记录完整性检查。

### 4.3 时间同步

| 起始地址 | 数量 | 类型 | 含义 |
|---:|---:|---|---|
| `0x000A` | 4 | ROS Time | 主站写入最近一次PPS边沿对应的UTC整秒；PPS存在时要求nsec=0 |

写入后，可通过 `0x0002..0x0005` 读取当前 UTC 时间，通过 `0x000A..0x000D` 读取最近一次同步值。

默认采用“一次性PPS对时，连续PPS维持UTC”的流程：

1. 上电首次对时：主站RTC在UTC整秒`N`输出PPS上升沿，随后通过`0x10`一次写入4个寄存器，发送`sec=N、nsec=0`。Health ready bit5为1后，确认时间同步成功。
2. 正常运行：主站继续每秒输出PPS，不再每秒写UTC。固件自动使PPS边沿UTC递增1秒，以TIM5产生毫秒、微秒，并根据PPS周期校正本地计时频率；保持时间同步，不进入周期性等待UTC。
3. PPS丢失：本采样窗口结束后停采，RS485保持响应。主站不得把保留的旧数据用于实时控制；没有PPS时无需尝试重新对时。
4. PPS恢复：在一次恢复PPS之后，重新写入该PPS对应的UTC整秒；如果错过本次写入窗口，应等待后续PPS并写入新的对应UTC，不得补发旧秒数。Health ready bit5恢复后，对时完成；使用绝对采样时间仍须检查本帧`UTC_VALID`。

固件将UTC绑定到已记录的PPS边沿，而不是报文接收时刻；内部时间精度为微秒。
主机RTC绝对时间调整时仍可主动写入对应PPS的UTC进行校正；旧主站每秒UTC确认也仍兼容，但不是正常运行的必要事务。

UTC报文必须对应最近的PPS，并在下一个PPS之前完成，不得跨秒发送积压的旧UTC报文；协议没有PPS序号，无法区分旧报文和主动时间校正。
同一PPS的重复UTC写入仍绑定该PPS边沿，不会改绑到接收时刻。所有`0x10`与`0x41`事务须串行，等待完整应答后再发送下一笔。

首次UTC对时前、PPS丢失或周期异常后，UTC有效性为0；相邻PPS的本地周期在900000～1100000 us范围内才视为连续。
PPS丢失检测仍为历史周期加50 ms（默认约1050 ms）。异常后即使PPS恢复，也需重新写入对应UTC才能恢复同步。
正常模式没有PPS时写入UTC只提供软件估算时间，不构成PPS对时。已经建立的UTC估算值可以保留，但不能仅凭非零时间戳判断有效；独立调试及模式切换期间拒绝UTC写入。
时间有效性与传感器数值有效性相互独立：未对时的新采样仍可`DATA_VALID=1、UTC_VALID=0`，无需丢弃数值。

联调检查：首次对时后持续PPS且停止UTC写入，确认后续新帧`UTC_VALID`持续为1、PPS边沿时间每秒增加1秒；断开PPS后确认停采和失效标志；恢复PPS但暂不写UTC，确认新采样的`UTC_VALID=0`，重新写入对应UTC后确认后续新帧恢复有效。
首次/恢复对时前采集的旧帧不会因之后的UTC写入而变成时间有效帧；当前PPS周期异常时也需重新写UTC，不能只检测物理断线。

### 4.4 IMU 校准四元数表

算法使用形式：

```text
calibrated_quat[imu] = C[imu] * raw_quat[imu] * M[imu]
```

校准区范围：`0x1154..0x1293`。第一版只运行时生效，不做掉电保存。

#### C 表

范围：`0x1154..0x11D3`，16 个 IMU，每个 IMU 一个四元数，占 8 个寄存器。

```text
c_addr = 0x1154 + i * 8, i = 0..15
```

#### M 表

范围：`0x11D4..0x1253`，16 个 IMU，每个 IMU 一个四元数，占 8 个寄存器。

```text
m_addr = 0x11D4 + i * 8, i = 0..15
```

#### 单个四元数寄存器顺序

| offset | 类型 | 含义 |
|---:|---|---|
| `+0` | U16 | w low word |
| `+1` | U16 | w high word |
| `+2` | U16 | x low word |
| `+3` | U16 | x high word |
| `+4` | U16 | y low word |
| `+5` | U16 | y high word |
| `+6` | U16 | z low word |
| `+7` | U16 | z high word |

写表时只进入 staging 缓冲区，不立即影响解算。写完整表后需要执行 apply。

使用 `0x06 Write Single Register` 写校准控制区时，主站应先写 `magic` 和 `seq`，最后写 `command`；写入 `command` 的那一帧会触发 apply/reset。

#### 校准控制区

| 地址 | 类型 | 读写 | 含义 |
|---:|---|---|---|
| `0x1254` | U16 | R/W | magic，执行命令前写 `0xCA1B` |
| `0x1255` | U16 | R/W | command：`1=apply`，`2=reset_identity` |
| `0x1256` | U16 | R/W | seq，主站递增序号 |
| `0x1257` | U16 | R | status |
| `0x1258` | U16 | R | error_index |
| `0x1259` | U16 | R | last_applied_seq |
| `0x125A..0x1293` | U16 | R | reserved，当前读 `0` |

status 定义：

| 值 | 含义 |
|---:|---|
| `0x0000` | idle |
| `0x0001` | applied |
| `0x0002` | reset_done |
| `0x8001` | bad_magic |
| `0x8002` | bad_cmd |
| `0x8003` | bad_quat |

error_index 定义：

- `0..15`：C 表对应 IMU 校验失败
- `16..31`：M 表对应 IMU 校验失败
- `0xFFFF`：无错误

执行 apply：

```text
Write Multiple Registers
start = 0x1254
count = 3
data  = 0xCA1B, 0x0001, seq
```

执行 reset_identity：

```text
Write Multiple Registers
start = 0x1254
count = 3
data  = 0xCA1B, 0x0002, seq
```

`0x06` 执行 apply 示例：

```text
Write Single Register 0x1254 = 0xCA1B
Write Single Register 0x1256 = seq
Write Single Register 0x1255 = 0x0001
```

`0x06` 执行 reset_identity 示例：

```text
Write Single Register 0x1254 = 0xCA1B
Write Single Register 0x1256 = seq
Write Single Register 0x1255 = 0x0002
```

校验规则：每个四元数分量必须为 finite float，且范数平方必须在 `0.25..2.25` 内。apply 成功后固件会归一化四元数，并更新算法当前校准表。命令处理完成后，`command` 和 `magic` 会自动清零；主站通过 `status/error_index/last_applied_seq` 判断结果。

## 5. 推荐读取方式

### 5.1 高频传感快照

上位机需要同时读取IMU、关节和触觉时，使用自定义功能码 `0x41`，避免标准
`0x03`受125个寄存器上限影响而产生5次串行往返。标准 `0x03/0x06/0x10`
仍可用于普通调试和寄存器访问。Schema 3增加PPS/采样状态与最后完整帧帧龄，旧版主站必须按`0x0012`升级解析长度。

请求帧：

```text
[slave][0x41][CRC_L][CRC_H]
```

响应帧：

```text
[slave][0x41][payload_len_H][payload_len_L][payload][CRC_L][CRC_H]
```

当前`payload_len`为906字节，载荷内每个寄存器仍按Modbus大端字节序排列：

| 载荷顺序 | 寄存器数 | 内容 |
|---|---:|---|
| 1 | 2 | FullFrame `frame_id`，低16位在前 |
| 2 | 4 | FullFrame时间戳，ROS Time的sec/nsec各按低16位在前 |
| 3 | 1 | snapshot status：bit0=数据有效，bit1=UTC有效，bit2=PPS存在，bit3=采样活动，bit4=独立调试 |
| 4 | 1 | IMU有效位 |
| 5 | 1 | 关节状态 |
| 6 | 1 | 触觉状态 |
| 7 | 1 | 最后一帧帧龄，单位ms，超过65535或从未产帧时为`0xFFFF` |
| 8 | 320 | IMU `0x1000..0x113F` |
| 9 | 54 | 关节 `0x1300..0x1335` |
| 10 | 68 | 触觉 `0x2000..0x2043` |

从站在构造响应前只抓取一次FullFrame快照，因此帧号、时间戳和三段传感数据
属于同一帧。完整响应为912字节，未超过当前1024字节RS485发送缓冲区。

正常模式检查snapshot status bit0和bit2及各传感器状态；独立调试（bit4=1）不要求bit2，但仍需检查数据有效及各传感器状态。UTC未同步时bit1为0，但传感器数据仍可有效；此时可使用`frame_id`和主站本地时间处理数据。首次对时成功后的连续正常PPS期间bit1不因等待UTC刷新而清零；首次/恢复对时前采集的帧即使晚于UTC写入才被读取，bit1也不会被误置为1。正常模式PPS丢失，或任一模式帧龄超过30ms时，bit0清零；载荷仍保留最后一帧，仅供显示和诊断。独立调试的PPS消失不会单独使数据失效。

8N1链路发送一帧912字节响应，在4 Mbps下约需2.28 ms，在6 Mbps下约需1.52 ms；
主站必须使用与烧录固件一致的波特率，并为200 us响应等待、请求接收、快照构造和收发方向切换留出余量。
推荐以8 ms周期、125 Hz轮询；工程内监控工具的默认传感器轮询也已改为125 Hz。
手套内部传感器和FullFrame在每个正常PPS周期内产生125帧，每次RS485响应返回当时最新的一帧。PPS上升沿
作为本秒第0帧，后续第1至124帧由TIM2产生；第124帧同步脉冲结束后TIM2停止，下一次
PPS到来前不再采样。PPS超过正常周期未到达时，传感器数据会停止更新并进入同步超时状态。
Linux主站建议同步改为8 ms轮询周期；继续按200 Hz读取不会提高真实采样率，只会更频繁地读取重复快照。
上位机必须等待完整响应后再发送下一条请求，不允许并发或流水发送请求。

`tools/modbus485_monitor.py`中的“125Hz Sensors”模式采用以下策略：

- `0x41`请求写入串口驱动后，响应容错窗口为8 ms；正常收到完整912字节后立即返回，不固定等待8 ms；
- 串口一次等待完整响应，避免短时间分片造成多次Windows/USB串口驱动往返；
- 高频事务不做周期内重试，超时后清空不完整响应并进入下一笔事务；
- 使用8 ms时基调度；事务超过当前周期时记录overrun，并立即从当前时刻重建节拍；
- Windows采集期间启用1 ms高精度定时器，并提高通信线程优先级；周期末尾2 ms采用忙等，降低线程晚唤醒；
- 只在相邻事务间隔不足时补足100 us静默时间，正常8 ms节拍不再额外忙等；
- 固件版本、设备信息和健康状态在高速轮询启动前读取并缓存，运行期间只发送`0x41`，避免标准寄存器事务打断125 Hz节拍；
- 界面分别显示通信响应频率、`frame_id`计算出的真实传感帧率、重复响应数和调度超期数。

上位机统计传感器更新率时，以约1秒窗口内的 `frame_id` 增量除以上位机单调
时钟的实际窗口长度。该统计不依赖UTC是否完成同步；连续响应中的 `frame_id`
相同表示本次通信读到了尚未更新的同一传感器帧。

### 5.2 标准寄存器读取

大块区域需要分包读取：

| 数据 | 地址范围 | 寄存器数 | 建议 |
|---|---:|---:|---|
| 基础状态 | `0x0000..0x000D` | 14 | 一次读 |
| 固件版本 | `0x000E..0x0010` | 3 | 一次读 |
| 设备信息 | `0x0011..0x001B` | 11 | 一次读 |
| 命令ACK | `0x0023..0x0025` | 3 | 执行命令后读取 |
| 系统状态 | `0x0040..0x0049` | 10 | 一次读 |
| 统一健康状态 | `0x004A..0x005F` | 22 | 一次读 |
| SD状态 | `0x0081..0x00BF` | 63 | 一次读 |
| IMU 数据 | `0x1000..0x113F` | 320 | 分包读，例如 100 + 100 + 100 + 20 |
| IMU 状态 | `0x1140..0x1144` | 5 | 一次读 |
| 关节数据 | `0x1300..0x1335` | 54 | 一次读 |
| 关节状态 | `0x1340..0x1346` | 7 | 一次读 |
| 触觉有效数据 | `0x2000..0x2043` | 68 | 一次读 |
| 触觉状态 | `0x2080..0x2087` | 8 | 一次读 |
| C 校准表 | `0x1154..0x11D3` | 128 | 分包读写 |
| M 校准表 | `0x11D4..0x1253` | 128 | 分包读写 |

## 6. 异常说明

- 访问未开放地址：返回 Modbus exception `0x02`，Illegal Data Address
- 读取退役地址`0x0060..0x0071`：返回 Modbus exception `0x02`
- 读写数量非法：返回 Modbus exception `0x03`，Illegal Data Value
- 写入非开放区域：返回 Modbus exception `0x02`
